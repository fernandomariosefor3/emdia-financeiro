[build]
  base = "artifacts/emdia"
  command = "corepack enable && corepack prepare pnpm@9 --activate && pnpm install --no-frozen-lockfile && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--version"
  PORT = "3000"
  BASE_PATH = "/"
